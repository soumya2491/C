#include <stdio.h>
#include <math.h>

int main() {
    float number;
    printf("Enter a number: ");
    scanf("%f", &number);
    printf("Ceil of %.2f is %.2f\n", number, ceil(number));
    printf("Floor of %.2f is %.2f\n", number, floor(number));

    return 0;
}
#include <stdio.h>

int main(){
    int n;
    return 0;
}