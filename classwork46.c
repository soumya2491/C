#include <stdio.h>
#include <stdlib.h>  
int main() {
    int num;

    printf("Enter a number: ");
    scanf("%d", &num);

    int abs_value = abs(num);

    printf("The absolute value of %d is %d", num, abs_value);

    return 0;
}
#include <stdio.h>

int main(){
    
    return 0;
}